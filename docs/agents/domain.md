# Domain Docs

Single-context repository.

Before exploring, read root `CONTEXT.md` and relevant ADRs under `docs/adr/` when present. Missing files are normal and need no action until domain decisions require them.
